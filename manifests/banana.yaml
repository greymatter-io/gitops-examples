apiVersion: apps/v1
kind: Deployment
metadata:
  name: banana
  annotations:
    greymatter.io/configure-sidecar: "false"
spec:
  selector:
    matchLabels:
      app: banana
  template:
    metadata:
      labels:
        app: banana
    spec:
      containers:
        - name: banana
          image: python:3
          command: ["python"]
          args: ["-m", "http.server", "42071"]
          ports:
          - name: ingress
            containerPort: 42071
