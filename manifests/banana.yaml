apiVersion: apps/v1
kind: Deployment
metadata:
  name: banana
  namespace: produce-aisle
  annotations:
    greymatter.io/configure-sidecar: "false"
spec:
  selector:
    matchLabels:
      app: banana
  template:
    metadata:
      labels:
        app: banana
    spec:
      containers:
        - name: banana
          image: python:3
          command: ["python"]
          args: ["-m", "http.server", "8080"]
